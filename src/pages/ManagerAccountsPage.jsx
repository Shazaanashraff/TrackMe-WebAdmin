import { PageHeader } from '@/components/shared/page-header';

// Intentionally blank. The vehicle-account password-reset UI that used to live
// here was cleared to make room for a new Drivers screen — build it out here.
export function ManagerAccountsPage() {
  return (
    <div className="space-y-6">
      <PageHeader title="Drivers" />
    </div>
  );
}
