# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Playwright CLI session artifacts (screenshots, console logs, page snapshots)
.playwright-cli/

# Local environment. VITE_* values are inlined into the built bundle, so a real
# key here is public once deployed — but it must still never enter git history.
# .env.example is the tracked template.
.env
.env.*
!.env.example
